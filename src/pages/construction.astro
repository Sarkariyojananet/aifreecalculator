---
import BaseLayout from '../layouts/BaseLayout.astro';
import CalculatorCard from '../components/CalculatorCard.astro';
import CategoryTabs from '../components/CategoryTabs.astro';
import AdSlotTop from '../components/ads/AdSlotTop.astro';
import AdSlotFooter from '../components/ads/AdSlotFooter.astro';
import { getCalculatorsByCategory } from '../data/calculators';

const constructionCalculators = getCalculatorsByCategory('Construction');
---

<BaseLayout
  title="Civil Construction & RCC BOQ Calculators - AI Free Calculator"
  description="Professional civil engineering calculators: RCC slab BBS, beam rebar, column steel, concrete mix breakup (M15/M20/M25), brickwork, plaster, and master house estimate BOQ."
>
  <section class="border-b border-slate-200 bg-white px-4 py-12 dark:border-slate-800 dark:bg-slate-900/60 sm:py-16">
    <div class="mx-auto max-w-7xl">
      <div class="flex items-center gap-2 text-xs text-blue-600 dark:text-blue-400 font-bold uppercase tracking-wider mb-2">
        <span>🏗️</span> CIVIL ENGINEERING SUITE
      </div>
      <h1 class="text-3xl font-black tracking-tight text-slate-900 dark:text-white sm:text-4xl">
        Construction & Civil BOQ Calculators
      </h1>
      <p class="mt-3 max-w-2xl text-sm leading-relaxed text-slate-600 dark:text-slate-300">
        Industry-standard estimation tools for structural engineers, contractors, site supervisors, and builders adhering to IS 456 / CPWD thumb rules.
      </p>
    </div>
  </section>

  <div class="mx-auto max-w-7xl px-4">
    <AdSlotTop />
  </div>

  <section class="px-4 py-10">
    <div class="mx-auto max-w-7xl space-y-6">
      <CategoryTabs activeCategory="construction" />

      <div class="grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-3">
        {constructionCalculators.map((calc) => (
          <CalculatorCard calculator={calc} />
        ))}
      </div>
    </div>
  </section>

  <div class="mx-auto max-w-7xl px-4">
    <AdSlotFooter />
  </div>
</BaseLayout>
